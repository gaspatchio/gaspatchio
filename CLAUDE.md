# CLAUDE.md

Claude Code reads `CLAUDE.md`; the canonical, cross-agent rules live in `AGENTS.md` files.
This shim imports the framework knowledge plus both subproject rule sets so a session
started at the repo root has everything loaded. Codex/Amp/Cursor read these `AGENTS.md`
files natively.

@AGENTS.md
@core/AGENTS.md
@bindings/python/AGENTS.md
